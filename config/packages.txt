# Curated overlay package set.
#
# The set of atoms the gentoo-ing image expects this binhost overlay to
# satisfy. It matches config/gap-build.txt: packages the official Gentoo
# binhost does not ship, built once here and published as prebuilt binpkgs.
#
# ci builds the binpkgs for everything on this list (tools/make-binpkg.sh uses
# config/gap-build.txt); these lines drive tools/seed-binhost.py, which for any
# atom the OFFICIAL binhost starts carrying can fetch the newest official
# .tbz2 into packages/ as a faster "plop-in" starting point.
#
# sys-apps/bootc, app-shells/gum, and dev-util/just have no ::gentoo ebuild at
# all — they can never be seeded; they exist only via the ebuilds/ overlay and
# are built here. seed-binhost.py reports "no binpkg found" for them.
#
# Format: one CPV or category/package prefix per line. '#' is a comment.

sys-apps/bootc
app-shells/gum
dev-util/just
sys-kernel/gentoo-kernel-bin
sys-kernel/installkernel
sys-kernel/linux-firmware
app-containers/skopeo
app-emulation/flatpak
net-wireless/iwd
app-misc/jq