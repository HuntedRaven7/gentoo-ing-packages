# Curated overlay package set.
#
# These are the packages the gentoo-ing image should always take from this
# binhost rather than the official one. Drop the matching .tbz2 files into
# packages/ (same category path as the official layout) and they win over the
# official binhost because this repo has higher priority (1 vs 9959).
#
# Anything NOT listed here still resolves from the official Gentoo binhost
# (sync-uri fallback) or falls back to a source build.
#
# Format: one CPV or category/package pattern per line. '#' is a comment.
# tools/seed-binhost.sh uses this list to pull the newest available build from
# the official binhost as a starting point.

sys-kernel/gentoo-kernel-bin
sys-kernel/linux-firmware
app-containers/podman
app-containers/skopeo
dev-util/ostree
sys-apps/systemd
sys-kernel/dracut
net-misc/openssh
net-wireless/iwd
app-admin/sudo
app-editors/vim
app-misc/tmux